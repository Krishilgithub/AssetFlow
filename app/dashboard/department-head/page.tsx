import { DeptHeadDashboard } from "@/components/sections/dept-head-dashboard";

export const metadata = {
  title: "Department Head — AssetFlow",
  description: "Department-level oversight, approvals, and asset monitoring console",
};

export default function DeptHeadPage() {
  return <DeptHeadDashboard />;
}
